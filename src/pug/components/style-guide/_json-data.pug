div(class='container')
	each item, index in slider.slider1
		div
			
			img(src=item.img, style='width:100px')
			p=item.title
hr
div(class='container') 
	each item, index in slider.slider2
		div
			img(src=item.img, style='width:100px')
			p=item.title
			p=item.category.cat1
			p=item.category.cat2

hr
div(class='container')
	each item, index in lpdata.topBullets
		div
			p=item.id
			p=item.number
			p=item.title
